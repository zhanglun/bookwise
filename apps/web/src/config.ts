export enum RouteConfig {
  HOME = "/",
  SEARCH = "/search",
  FILTER = "/filter",
  LIBRARY = "/library",
  EDITOR = "/editor/:id",
  ALL = "/all",
  READING = "/reading",
  FINISHED = "/finished",
}
